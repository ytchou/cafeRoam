{
  "name": "caferoam",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild:pass0": "tsx scripts/prebuild/data-pipeline/pass0-seed.ts",
    "prebuild:pass1": "tsx scripts/prebuild/data-pipeline/pass1-verify.ts",
    "prebuild:pass2": "tsx scripts/prebuild/data-pipeline/pass2-scrape.ts",
    "prebuild:all": "pnpm prebuild:pass0 && pnpm prebuild:pass1 && pnpm prebuild:pass2",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "echo 'No ESLint configured for prebuild-only package'",
    "format:check": "echo 'No Prettier configured for prebuild-only package'",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "apify-client": "^2.11.0",
    "fuzzball": "^2.2.3"
  }
}
