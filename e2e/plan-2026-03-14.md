# E2E Test Plan — 2026-03-14
**Branch:** main
**Run type:** Full re-run (all scenarios stale after Phase 2B commits — camelCase API, rebuilt UI)

## Stale Scenarios (all re-tested)
- Anonymous Browse + Auth Wall — SearchBar rebuilt, /search is new auth-gated route
- Signup + PDPA Consent — was FAIL (consent 401), camelCase API change
- Map Page Public Access — massive overhaul: MapView, desktop card, viewport filter
- List Management + Cap Enforcement — was BLOCKED (consent 401)
- Account Deletion — was BLOCKED (consent 401)

## New Scenario Added
- Shop Detail Public Access — new SSR route /shops/[shopId]/[slug], not previously covered

---

## Scenarios

### 1. Anonymous Browse + Auth Wall
- **Actor:** anonymous
- **Persona:** Yuki
- **Goal:** Verify public routes load freely and auth-gated routes redirect to login
- **Pre-conditions:** not logged in
- **Steps:**
  1. Navigate to `/` — assert page loads and SearchBar visible
  2. Type "quiet pour-over near Zhongshan" in search bar — submit
  3. Assert redirect to `/login` OR inline "sign in to search" prompt appears
  4. Navigate to `/lists` directly — assert redirect to `/login`
  5. Navigate to `/search` directly — assert redirect to `/login`
- **Expected outcome:** auth wall enforced on semantic search and protected routes
- **Risk if broken:** unauthenticated users access protected features
- **DB state needed:** none

---

### 2. Map Page Public Access
- **Actor:** anonymous
- **Persona:** Yuki (first-time visitor)
- **Goal:** Map loads publicly, pins render, clicking a pin shows shop info
- **Pre-conditions:** not logged in; at least 1 live shop in DB
- **Steps:**
  1. Navigate to `/map` — assert map container renders (no redirect to login)
  2. Wait for pins to load — assert at least 1 shop pin visible on map canvas
  3. Click a pin — assert popup or card with shop name appears
  4. Assert no login prompt appears during browsing
- **Expected outcome:** map fully usable without auth
- **Risk if broken:** core discovery broken; blank map or auth redirect for anonymous users
- **DB state needed:** at least 1 seeded shop with lat/lng

---

### 3. Shop Detail Public Access *(NEW)*
- **Actor:** anonymous
- **Persona:** Yuki (following a shared link)
- **Goal:** SSR shop detail loads correctly; invalid slug redirects; missing shop 404s
- **Pre-conditions:** not logged in; at least 1 seeded shop with slug
- **Steps:**
  1. Navigate to `/api/shops` to get a valid shopId + slug from seed data
  2. Navigate to `/shops/[shopId]/[slug]` — assert shop name and description visible
  3. Navigate to `/shops/[shopId]/wrong-slug` — assert canonical redirect to correct slug OR 404
  4. Navigate to `/shops/00000000-0000-0000-0000-000000000000/nonexistent` — assert 404 page
- **Expected outcome:** shop detail loads publicly; slug redirect works; 404 for missing shops
- **Risk if broken:** shared links broken; SSR errors or missing 404 handling
- **DB state needed:** at least 1 seeded shop with slug populated

---

### 4. Signup + PDPA Consent
- **Actor:** anonymous → newly registered
- **Persona:** Yuki (signing up for first time)
- **Goal:** Full signup flow works; consent recorded; auth session established
- **Pre-conditions:** not logged in; fresh test email
- **Steps:**
  1. Navigate to `/signup` — assert form visible with email, password, PDPA checkbox
  2. Fill in unique test email + password — assert fields populated
  3. Check PDPA checkbox — assert checkbox checked
  4. Submit — assert redirect to `/onboarding/consent` or PDPA page
  5. On consent page — accept PDPA — assert redirect to `/`
  6. Assert header nav shows "我的" (logged-in state), not "登入"
  7. Save storageState.json for reuse in subsequent scenarios
- **Expected outcome:** account created, consent recorded, user lands authenticated on home
- **Risk if broken:** no new users can register; PDPA compliance gap
- **DB state needed:** none (creates new rows)

---

### 5. Authenticated Search
- **Actor:** authenticated
- **Persona:** Mei-Ling (searching for a workspace)
- **Goal:** Semantic search returns results for authenticated user; camelCase API renders correctly
- **Pre-conditions:** logged in (storageState.json from Scenario 4 or existing session)
- **Steps:**
  1. Navigate to `/` — assert header shows "我的" (authenticated)
  2. Type "specialty coffee" in search bar — submit or navigate to `/search?q=specialty+coffee`
  3. Assert search results list renders with at least 1 shop card
  4. Assert shop cards show name and attributes (verifies camelCase API renders correctly)
  5. Click the first result — assert shop detail page loads with shop name visible
- **Expected outcome:** authenticated search returns results; shop cards render with Phase 2B camelCase API
- **Risk if broken:** core search broken post camelCase migration
- **DB state needed:** at least 1 seeded shop with vector embeddings

---

### 6. List Management + Cap Enforcement
- **Actor:** authenticated
- **Persona:** Mei-Ling (power user)
- **Goal:** Can create up to 3 lists; 4th creation attempt rejected with error
- **Pre-conditions:** logged in; user has 0 existing lists
- **Steps:**
  1. Navigate to `/lists` — assert empty state shown
  2. Create list "Specialty Only" — assert appears in list
  3. Create list "Work Spots" — assert appears
  4. Create list "Weekend Picks" — assert 3 lists total
  5. Attempt to create 4th list "Too Many" — assert error message appears
  6. Assert 4th list does NOT appear
  7. Navigate into first list — click "Add shop" on a seed shop — assert shop added
- **Expected outcome:** 3-list cap enforced; 4th rejected with visible error; adding shops works
- **Risk if broken:** business rule violation; unlimited lists in DB
- **DB state needed:** authenticated user with 0 lists; at least 1 seeded shop

---

### 7. Account Deletion *(run last — destroys auth state)*
- **Actor:** authenticated
- **Persona:** any
- **Goal:** Soft-delete sets grace period; user sees correct messaging
- **Pre-conditions:** logged in (storageState.json from Scenario 4)
- **Steps:**
  1. Navigate to `/settings` — assert settings page visible
  2. Click "Delete account" — assert confirmation prompt appears
  3. Confirm deletion — assert success message mentions 30-day grace period
  4. Assert user is NOT immediately logged out
- **Expected outcome:** deletion_requested_at set; grace period message shown; account not immediately deleted
- **Risk if broken:** immediate data deletion; PDPA violation
- **DB state needed:** none (modifies user from Scenario 4)

---

### BLOCKED: Search + Check-in
- **Reason:** e2e/fixtures/test-photo.jpg does not exist
- **Fix:** add any JPEG to e2e/fixtures/test-photo.jpg (min 10KB) then re-run

---

## Execution Order
1 (anon auth wall) → 2 (map) → 3 (shop detail) → 4 (signup) → 5 (search) → 6 (lists) → 7 (deletion)
