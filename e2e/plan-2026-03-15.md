# E2E Test Plan — 2026-03-15
**Branch:** fix/e2e-smoke-failures
**Purpose:** Re-run all 7 scenarios to validate fixes from PR #35. Scenario 8 (Check-in) is newly unblocked.

---

## Scenarios

### 1. Anonymous Browse + Auth Wall
- **Actor:** anonymous
- **Persona:** Yuki (first-time visitor)
- **Goal:** Verify unauthenticated users can browse shops but cannot access search, lists, or check-ins
- **Pre-conditions:** not logged in
- **Steps:**
  1. Navigate to `http://localhost:3000/` — assert page loads, search bar visible
  2. GET `http://localhost:3000/api/search?q=coffee` — assert 401 (auth wall)
  3. Navigate to `http://localhost:3000/lists` — assert 307 redirect to `/login`
  4. Navigate to `http://localhost:3000/search` — assert 307 redirect to `/login`
- **Expected outcome:** Home loads publicly; search/lists/search all redirect or 401 unauthenticated
- **Risk if broken:** Users can bypass auth wall
- **DB state needed:** none

---

### 2. Shop Detail Public Access *(was FAIL — fixes applied)*
- **Actor:** anonymous
- **Persona:** Yuki
- **Goal:** View a shop's detail page without auth
- **Pre-conditions:** not logged in, slugs backfilled (710 shops have slugs)
- **Steps:**
  1. GET `http://localhost:8000/shops/` (first shop) — capture `id` and `slug` from first result
  2. Navigate to `http://localhost:3000/shops/{id}/{slug}` — assert 200, shop name visible
  3. Navigate to `http://localhost:3000/shops/{id}/wrong-slug` — assert canonical redirect (302) to correct slug
  4. Navigate to `http://localhost:3000/shops/00000000-0000-0000-0000-000000000000/nope` — assert 404 page
- **Expected outcome:** Shop detail loads (200), slug redirect works, missing shop 404s cleanly
- **Risk if broken:** Core product page broken — users can't view shops
- **DB state needed:** none (slugs already backfilled)

---

### 3. Signup + PDPA Consent *(was FAIL — proxy.ts fix in worktree)*
- **Actor:** anonymous, completing signup
- **Persona:** Yuki
- **Goal:** Create an account and record PDPA consent
- **Pre-conditions:** not logged in, fresh test email
- **Steps:**
  1. Navigate to `http://localhost:3000/signup` — assert signup form visible
  2. Fill email `e2e-test-${Date.now()}@example.com` and password `TestP@ss1234!`
  3. Submit — assert redirect to `/consent` page
  4. On consent page, click "I accept" / consent button — assert POST `/api/auth/consent` returns 200
  5. Assert redirect to `/` home page with authenticated state
- **Expected outcome:** Account created, consent recorded, user lands on home
- **Risk if broken:** New users stuck at consent loop, can't access app
- **DB state needed:** none (creates new user)

---

### 4. Map Page Public Access
- **Actor:** anonymous
- **Persona:** Yuki
- **Goal:** Verify map page loads and shop data is accessible
- **Pre-conditions:** not logged in
- **Steps:**
  1. Navigate to `http://localhost:3000/map` — assert 200 (no redirect to login)
  2. GET `http://localhost:8000/shops/?featured=true&limit=200` — assert ≥1 shop with lat/lng
- **Expected outcome:** Map page loads publicly, shop data present
- **Risk if broken:** Discovery page inaccessible
- **DB state needed:** none

---

### 5. List Management + Cap Enforcement *(was PARTIAL FAIL — false alarm confirmed)*
- **Actor:** authenticated
- **Persona:** Mei-Ling
- **Goal:** Create lists up to cap, verify 4th is rejected, add a shop to a list
- **Pre-conditions:** logged in as `admin@caferoam.local` (storageState), user has 0 existing lists
- **Steps:**
  1. DELETE any pre-existing lists for the test user via API to reset state
  2. POST `/api/lists` `{"name":"Specialty Only"}` — assert 201
  3. POST `/api/lists` `{"name":"Work Spots"}` — assert 201
  4. POST `/api/lists` `{"name":"Weekend Picks"}` — assert 201
  5. POST `/api/lists` `{"name":"Fourth List"}` — assert 400 with "maximum 3 lists" message
  6. GET `/api/lists` — assert exactly 3 lists returned
  7. POST `/api/lists/{firstListId}/shops` `{"shop_id":"<valid-shop-id>"}` — assert 201
- **Expected outcome:** 3 lists created; 4th rejected with 400; shop added to list with 201
- **Risk if broken:** Users can bypass list cap; shop collection broken
- **DB state needed:** test user must have 0 lists (step 1 resets)

---

### 6. Authenticated Search
- **Actor:** authenticated
- **Persona:** Mei-Ling
- **Goal:** Semantic search returns results with correct camelCase shape
- **Pre-conditions:** logged in (storageState)
- **Steps:**
  1. GET `/api/search?q=specialty+coffee` with auth — assert 200, ≥1 result
  2. Assert first result has `photoUrls`, `modeScores`, `taxonomyTags` fields (camelCase)
- **Expected outcome:** Search returns camelCase-shaped results
- **Risk if broken:** Search page broken
- **DB state needed:** none

---

### 7. Account Deletion
- **Actor:** authenticated
- **Persona:** Any user
- **Goal:** Request account deletion sets grace period, doesn't immediately delete
- **Pre-conditions:** logged in (fresh user for this test — don't delete the main test user)
- **Steps:**
  1. Create a throwaway user via signup
  2. DELETE `/api/auth/account` with auth — assert 200
  3. Assert response contains `deletion_requested_at` timestamp
  4. GET `/api/auth/me` or profile — assert user still exists (not immediately deleted)
- **Expected outcome:** deletion_requested_at set, account exists during grace period
- **Risk if broken:** PDPA compliance at risk
- **DB state needed:** throwaway user (created in test)

---

### 8. Search + Check-in *(was BLOCKED — fixture now present)*
- **Actor:** authenticated
- **Persona:** Mei-Ling
- **Goal:** Complete a full check-in flow with photo upload
- **Pre-conditions:** logged in (storageState), `e2e/fixtures/test-photo.jpg` present, ≥1 live shop in DB
- **Steps:**
  1. GET `/api/search?q=coffee` — capture a shop ID from results
  2. Navigate to `http://localhost:3000/shops/{id}/{slug}` — assert shop detail loads
  3. Click "Check In" button — assert check-in form/modal opens
  4. Upload `e2e/fixtures/test-photo.jpg` — assert photo preview appears
  5. Submit check-in form — assert 200/201, no 422
  6. Assert stamp toast notification appears
- **Expected outcome:** Check-in recorded in DB, stamp awarded, toast visible
- **Risk if broken:** Core monetizable mechanic broken
- **DB state needed:** fresh seed or live shop in DB; test-photo.jpg in e2e/fixtures/
