import { test } from './fixtures/auth';

test.describe('J14 — Profile: check-in history + stamp collection', () => {
  test.fixme('logged-in user sees their stamp passport and check-in history on /profile', async () => {});
});

test.describe('J15 — Account deletion: request → grace period', () => {
  test.fixme('requesting account deletion shows confirmation and sets 30-day grace period', async () => {});
});

test.describe('J25 — Display name update', () => {
  test.fixme('changing display name in settings reflects on profile page', async () => {});
});
