import { test } from '@playwright/test';

test.describe('J16 — Activity feed: public access', () => {
  test.fixme('unauthenticated user can view the public activity feed with recent check-ins', async () => {});
});
