# E2E Smoke Report — 2026-03-15
**Branch:** main (post PR #35 merge)
**Method:** API-level (browser unavailable — Chrome single-instance conflict; auto-fallback per updated skill)
**Summary:** 8 passed · 0 failed · 0 blocked

---

## [PASS] Scenario 1 — Anonymous Browse + Auth Wall
`/api/search` unauthenticated → 401. `/lists` → 307 redirect. `/search` → 307 redirect. `/checkin/test` → 307 redirect. Auth wall fully enforced on all protected routes.

---

## [PASS] Scenario 2 — Shop Detail Public Access
`GET /shops/{id}` returns 200 with all expected camelCase fields: `photoUrls`, `modeScores`, `taxonomyTags`, `slug`. Frontend `/shops/{id}/{slug}` returns 200. Wrong slug → 307 canonical redirect. Non-existent shop → 404 cleanly. All slug mechanics working correctly on main.

---

## [PASS] Scenario 3 — Signup + PDPA Consent
Fresh user created via `POST /auth/v1/signup`. `POST /api/auth/consent` with `Authorization: Bearer {token}` returns 200 with `pdpa_consent_at` set to a valid timestamp. Authorization header forwarding confirmed working on main (PR #35 fix validated).

---

## [PASS] Scenario 4 — Map Page Public Access
`GET /map` returns 200 without auth redirect. `GET /shops/?featured=true&limit=5` returns 5 shops with `latitude` fields present. Public map access and shop geo data confirmed.

---

## [PASS] Scenario 5 — List Management + Cap Enforcement
3 lists created successfully (200 each). 4th list attempt → 400 `{"detail":"Maximum of 3 lists allowed"}`. Shop added to list 1 via `POST /api/lists/{id}/shops` with `{"shop_id":"..."}` → 200. Cap enforcement and shop-add both working.

**Note:** Lists domain uses `snake_case` throughout (`shop_id` not `shopId`) — consistent with last run's finding.

---

## [PASS] Scenario 6 — Authenticated Search
`GET /api/search?text=specialty+coffee` returns 200 with 20 results. Response shape is `[{"shop": {...}}, ...]`. camelCase fields confirmed present: `photoUrls`, `modeScores`, `taxonomyTags` (in shops that have tags). Search returns results and correct field contract holds.

---

## [PASS] Scenario 7 — Account Deletion
Throwaway user created via signup. `DELETE /api/auth/account` → 200 with `deletion_requested_at` timestamp set. Account exists during grace period (not immediately deleted). PDPA grace-period logic working.

---

## [PASS] Scenario 8 — Search + Check-in
`POST /api/checkins` with `photo_urls` array → 200. `is_first_checkin_at_shop: true` in response. Stamp confirmed in DB (`GET /api/stamps` returns matching `shop_id`). Core check-in and stamp award pipeline working.

Photo upload to Supabase Storage (`checkin-photos` bucket) **requires path format `{user_id}/filename.jpg`** to satisfy RLS — uploads to arbitrary paths return 403. This is correct security behavior. Confirmed with user_id path → 200.

**UNVERIFIED (browser-only):** Stamp toast visual notification — cannot verify without browser session.

---

## Observations & Follow-up

1. **Photo URL validation gap (non-blocking):** Check-in API accepted a submission with an invalid/empty photo URL (`"checkin-photos/"`) without rejecting it. The service validates `photo_urls` is non-empty but not that URLs actually resolve. Consider adding URL format validation at the service layer.

2. **Stamp toast:** Still unverified at browser level. Close Chrome and retry `/e2e-smoke` when browser is available to confirm visual flow.

3. **`admin@caferoam.local` seed user missing:** DB was reset at some point. Run `pnpm db:seed` to restore. This test used a dynamically created user as a workaround.

4. **Search response wrapper:** Results are `[{"shop": {...}}, ...]` — `shop` is nested inside a wrapper object. Confirmed camelCase fields are inside `shop`. No regression.
