import { test } from '@playwright/test';

test.describe('J20 — Near Me: coords outside Taiwan → boundary behavior', () => {
  test.fixme('using geolocation with Tokyo coordinates shows appropriate fallback or empty state', async () => {});
});
